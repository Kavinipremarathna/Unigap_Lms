import { Achievement } from "@/lib/types";

// Tested mock data removed. All achievements are queried directly from PostgreSQL database via /api/achievements.
export const achievements: Achievement[] = [];
